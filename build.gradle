buildscript {
  repositories {
    jcenter()
  }
  dependencies {
    classpath 'org.ajoberstar:gradle-defaults:0.7.0-rc.4'
  }
}

plugins {
  id 'java'
}

apply plugin: 'org.ajoberstar.defaults'

group = 'org.ajoberstar'

repositories {
  jcenter()
}

dependencies {
  testCompile 'junit:junit:4.12'
}

model {
  bintray {
    owner = 'ajoberstar'
    repo = 'test'
    pkg = 'test-repo'
  }
}

wrapper {
  gradleVersion = '3.2.1'
}
