buildscript {
	repositories {
		mavenLocal()
		jcenter()
		mavenCentral()
	}
	dependencies {
		classpath 'org.ajoberstar:gradle-git:1.6.0-rc.5'
	}
}

apply plugin: 'org.ajoberstar.github-pages'

githubPages {
	pages {
		from 'src/main/pages'
	}
}

wrapper {
	gradleVersion = '3.1'
}
