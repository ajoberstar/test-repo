buildscript {
  dependencies { classpath 'org.ajoberstar:grgit:1.8.0-rc.1' }
}

plugins {
	id 'org.ajoberstar.grgit' version '1.6.0'
	id 'org.ajoberstar.git-publish' version '0.1.0'
}

gitPublish {
	branch = 'gh-pages'
	contents.from 'src/main/pages'
}

wrapper {
	gradleVersion = '3.2.1'
}
